[project]
name = "eurybis"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.129.0",
    "hypercorn>=0.18.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
    "ty>=0.0.17",
]
